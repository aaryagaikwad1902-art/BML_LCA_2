# BML_LCA_2
TYCSE-B, LCA-2 submission that pulls data directly from the UCI Machine Learning Repository, applies data preprocessing, trains a K-Nearest Neighbors (KNN) classifier, and calculates the model's performance accuracy
